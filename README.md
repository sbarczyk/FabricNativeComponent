# Fabric Native WebView Component

A React Native native component for Android (Java) and iOS (Objective-C++) built using the New Architecture (Fabric). It demonstrates JSI-based communication and Codegen integration by loading a web page and displaying a native alert upon successful script execution.
